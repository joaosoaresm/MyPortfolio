---
---

<section id="hero">
  <div class="hero-tag">Desenvolvedor & Fundador da Inbelt Tech</div>
  <h1 class="hero-name">
    Soares<br>
    <span class="line2">dev.</span>
  </h1>
  <p class="hero-desc">
    Desenvolvedor apaixonado por construir soluções.<br>
    Fundador da Inbelt Tech — manutenção e suporte<br>
    técnico pra quem precisa de resultado de verdade.
  </p>
  <div class="hero-ctas">
    <a href="#projetos" class="btn btn-accent">Ver projetos</a>
    <a href="#inbelt" class="btn">Inbelt Tech →</a>
  </div>
  <div class="hero-bg-text">DEV</div>
</section>

<style>
  #hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 8rem 3rem 4rem;
    max-width: 1100px;
    margin: 0 auto;
    position: relative;
    overflow: hidden;
  }
  .hero-tag {
    font-family: 'DM Mono', monospace;
    font-size: 0.75rem;
    color: #C84B31;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 1.5rem;
    animation: fadeUp 0.7s 0.2s ease both;
  }
  .hero-name {
    font-family: 'Syne', sans-serif;
    font-size: clamp(3.5rem, 8vw, 7rem);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.03em;
    margin-bottom: 1.8rem;
    animation: fadeUp 0.7s 0.35s ease both;
  }
  .hero-name .line2 { color: #6B6760; }
  .hero-desc {
    font-family: 'DM Mono', monospace;
    font-size: 0.9rem;
    color: #6B6760;
    line-height: 1.8;
    margin-bottom: 2.8rem;
    animation: fadeUp 0.7s 0.5s ease both;
  }
  .hero-ctas {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    animation: fadeUp 0.7s 0.65s ease both;
  }
  .btn {
    font-family: 'DM Mono', monospace;
    font-size: 0.78rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.75rem 1.6rem;
    border: 1.5px solid #141210;
    background: transparent;
    color: #141210;
    text-decoration: none;
    transition: background 0.2s, color 0.2s;
  }
  .btn:hover { background: #141210; color: #F7F5F0; }
  .btn-accent { background: #C84B31; color: #fff; border-color: #C84B31; }
  .btn-accent:hover { background: #a83825; border-color: #a83825; }
  .hero-bg-text {
    position: absolute;
    right: -1rem;
    bottom: 4rem;
    font-family: 'Syne', sans-